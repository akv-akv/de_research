apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: root-app
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: default
    name: in-cluster
  project: default
  source:
    path: kubentenes/apps  # contains application spec files
    repoURL: https://github.com/akv-akv/de_research.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true